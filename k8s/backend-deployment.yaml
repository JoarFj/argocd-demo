apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  labels:
    app: backend-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend-app
  template:
    metadata:
      labels:
        app: backend-app
    spec:
      containers:
      - name: backend-app
        image: ghcr.io/joarfj/argocd-demo-backend:082a9393c3c523443eb4e9e6e22f9cecd592d2be # Placeholder, will be updated by CI/CD
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080